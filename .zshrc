
export NVM_DIR="/Users/isaacbraun/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
alias python=/usr/bin/python3

# bun completions
[ -s "/Users/isaacbraun/.bun/_bun" ] && source "/Users/isaacbraun/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# Aliases

# Default directory
#cd dev
